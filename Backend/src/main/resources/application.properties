#spring.application.name=demo
spring.application.name=Ecommerce
server.port=8080
# =======================
# Database Connection
# =======================
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_slim?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=dong2004!

# =======================
# JPA / Hibernate
# =======================

# Show SQL logs in console
spring.jpa.show-sql=true
# Update schema automatically in dev (use 'validate' or migrations in prod)
spring.jpa.hibernate.ddl-auto=update
# (Optional) pretty-print SQL
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Option A (recommended): let Spring auto-detect the dialect (remove the next line)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Option B (explicit dialect—works too)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# =======================